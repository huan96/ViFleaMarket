<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.haui.huantd.vifleamarket">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.SignInActivity" />
        <activity android:name=".activities.InfoActivity" />
        <activity android:name=".activities.list_activity_add_product.AddProductActivity" />
        <activity android:name=".activities.ShowProductActivity" />
        <activity android:name=".activities.list_activity_add_product.DanhMucActivity" />
        <activity android:name=".activities.list_activity_add_product.LoaiSanPhamActivity" />
        <activity android:name=".activities.list_activity_add_product.KhuVucActivity" />
        <activity android:name=".activities.list_activity_add_product.HuyenActivity" />
        <activity android:name=".activities.list_activity_add_product.AddImagesActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.list_activity_add_product.GiaActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".activities.list_activity_add_product.TieuDeActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".activities.list_activity_add_product.MoTaChiTietActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".activities.ShowListProductActivity"></activity>
    </application>

</manifest>